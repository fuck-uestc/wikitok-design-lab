# 长文与切片适配 · 升级与使用

本版直接修改上传的正式工程，不是把静态 demo 接在外面。版本号 2.0.0。前台、后台、REST、批量导入、版本恢复、MCP 使用同一套内容和投放规则。没有修改远程仓库或部署现网。

## 1. 先保护现有数据

在**旧版工程仍在运行时**执行原有 `npm run db:backup`（已构建环境可用 `node dist/server/backend/src/cli.js backup`），核对输出 manifest 的 integrity 为 ok，并保存旧版源码、原 .env 和备份目录。暂停管理员编辑及导入后再切换版本，以免数据库快照之后又增加媒体文件。

把本版源码解压到新目录。保留现网的 `.env`、SQLite 数据库、上传目录和备份目录，不要用空目录覆盖。不要直接拷贝 Windows 的 node_modules 到 Linux；在目标系统重新 `npm ci`。Docker 要保留原 Compose 项目名和已有命名卷，不能执行 `docker compose down -v`。

数据库迁移 2 是增量添加 format、feed_ids、short_json、索引及站点设置表。旧条目保留 UUID、slug、kind、发布时间和历史版本；缺省成为长文，投放到 main/wiki。没有将正文改写为短内容。首次启动会自动执行迁移。

回滚应停服务，恢复升级前整套数据库与上传目录备份，再启动旧代码；不要对已升级的活动数据库手动 DROP COLUMN，也不要混用旧程序与新库写入。

## 2. 安装与启动

使用 Node.js 24.x。PowerShell 将下面的 npm 换成 npm.cmd。

```sh
npm ci
npm run setup -- --upgrade
npm run typecheck
npm test
npm run build
npm start
```

新目录没有 .env 时 setup 创建它并生成独立管理员密码与 MCP_TOKEN；有 .env 时 `--upgrade` 只补充缺少的键。已存在的空值也不会被擅自替换。调整 PUBLIC_SITE_URL、COOKIE_SECURE、代理和数据路径后启动。正式 HTTPS 部署见 docs/DEPLOYMENT.md。

本地开发可以在安装和配置后直接 `npm run dev`。前台 localhost:5173，后台 /admin，MCP /mcp。生产 `npm start` 默认监听后端 3001；不能把 5173 的开发地址原样用作生产 PUBLIC_SITE_URL。

**本交付环境的验证边界：** TypeScript 类型检查与 33 项自动化测试通过；后端 tsc 编译通过。Vite 生产构建因离线环境缺少 @rollup/rollup-linux-x64-gnu 而未完成。浏览器采用受限环境测试方式，见 QA_REPORT.md。不含 dist，也不能把截图或 QA 临时 bundle 当成生产构建通过的证明。目标机器安装依赖后务必运行 npm run check。

## 3. 混排与分开浏览

| 默认流 | 访问路径 | 接收类型 |
| --- | --- | --- |
| 发现 main | /f/main | long + short |
| 长文 wiki | /f/wiki | long |
| 切片 slices | /f/slices | short |

每条内容出现的条件：**已发布 × 投放到该流 × 格式被该流允许**。后台「站点与账号 → 信息流与混排」可以修改名称、允许类型、启用状态及默认首页；编辑器右侧设置该条目的投放位置。

只想在子流展示某条短内容：仅勾选 slices，不勾选 main。想同一条同时进入发现和短内容流：勾选 main、slices。想发现页只看长文：把 main 的允许格式改成仅 long；原有短内容及投放关系不删除，重新允许 short 后会恢复。

所有投放取消（feedIds=[]）只移出浏览流；**已发布的内容仍可以通过全站索引、全局搜索、收藏或永久链接访问**。隐藏内容须改为草稿或归档。信息流不是权限隔离或私有分区。

每个流的搜索、随机、分类和分页使用一致的过滤条件。搜索默认当前流，可主动切换全站；收藏是全站的。修改流配置后，已有浏览器页面刷新以更新导航；旧游标失效时重新加载第一页。

流最多 12 个；不要为了改显示名称而改 ID。ID 是条目引用的标识，改名不会自动迁移投放关系。临时不用请关闭「启用」。设置保存到 SQLite，优先于用于初始配置的 DEFAULT_FEED/FEEDS_JSON。

## 4. 长文与四种切片

长文仍用标题、摘要与 Markdown 正文，可选封面；无图不再占一个装饰占位版块。切片保留一个索引与分享标题，但原话卡首屏以原话为主，不重复显示摘要。

quote 展示逐字原话；note 明确标注编者概述；image 使用上传图片或合法外链，不裁成大背景；comparison 为两段内容分别关联来源。短内容的背景、来源和更正放在材料抽屉。超过一屏的原话允许自然滚动，不截断成省略号，也不制造内外双滚动。

核对状态由编辑者填写；「已与来源核对」不等于系统证实事实。source-checked 需要链接或文件；对照卡的两侧都要有可关联来源。来源最多 8 项，媒体 ID 必须先上传获得。更正状态必须填写说明。

旧 kind 分类继续用于主题，新的 format 决定长短，不把它们混为一谈。JSON/CSV/Markdown 批量导入、导出及历史恢复均保留新字段。导入 update 是完整替换，省略字段会应用默认值；只改投放请用 MCP artifact_patch。

## 5. MCP 改动（升级时必看）

现有部署若尚未配置 MCP_TOKEN 或 MCP_READ_TOKEN，会继续接受当前管理员密码作为 MCP Bearer Token，现有客户端无需改凭据。`npm run setup -- --upgrade` 会为缺少 MCP_TOKEN 的旧配置补充一个可选的独立随机凭据；如需迁移，可将客户端改为 `Authorization: Bearer <MCP_TOKEN 的值>`，然后在全部客户端切换后关闭密码兼容。

MCP_READ_TOKEN 可选，提供只读管理权限，**包括读取草稿与归档资料**。它不是可以公开分享的访客密钥。两种 token 均不进入公开配置或后台响应。后台仅显示地址和启用状态。

当配置了任一独立 token 时，`MCP_ALLOW_ADMIN_PASSWORD` 默认 false；显式设为 true 后才继续接受管理员密码。修改管理员密码会立即撤销密码兼容认证，不会撤销独立 token。完整工具与请求例子见 docs/MCP.md。

## 6. 设计取舍与未包含的能力

保留暖白/深色两套主题、细线分隔、页码、侧栏、原生滑动、键盘操作、索引、收藏及来源抽屉。移除了内容重复标题、无图大占位、图片胶片轨迹和短条目不需要的沉浸看图工具栏；没有去掉管理、来源或历史能力。

当前实现使用原生可变高度卡片，以避免长引文被截断；已经加载的卡片保留在 DOM，图片懒加载。**不是无限量 DOM 虚拟化实现**，需要一次连续浏览数千条时，应后续增加基于测量高度的窗口化并测试滚动恢复。分页 API 已支持游标。

没有新增 OAuth 登录、评论/点赞、推荐算法、多租户权限，也没有把 feed 当作独立数据库子站。独立域名可以由部署层作为另一个入口指向同一服务，但具体域名与 TLS/代理没有替你修改；正式链接通过 PUBLIC_SITE_URL 生成。需要独立隐私权限时应单独部署，不能只依赖 feedIds。

## 7. 验收建议

在备份副本先升级；核对旧长文、附件和历史；创建四种切片；在后台取消 main 投放，确认从发现流消失且 slices 可见；取消全部投放，确认公开直链仍能访问；改草稿，确认公开直链/附件失效（附件不能同时被其他已发布条目引用）；用只读 MCP token 读取及尝试写入，确认写入被拒绝；最后在真实域名测试刷新、浏览器前进后退、文件上传、来源图片、Cookie、反向代理与移动端滑动。
