# 设计说明

> 本文保留两套原型的设计依据。当前正式产品同时提供 MARGIN 与 DRIFT，由访客选择并记忆偏好；两套布局共用校园 BBS Wiki 数据、收藏和阅读状态。原文中仅选一套、仅接 Wikipedia 等建议属于原型阶段，不是当前实现限制。

## 共同原则：浏览先轻，阅读后深

首屏只有一条内容：主题、标题、两三行摘要、展开、收藏。不要在首屏加入评论区、排行榜、用户头像、长目录或搜索结果网格。用户打开网站就开始读，不必先经过一页产品宣传。

把内容分成三个深度：卡片摘要 → 页内阅读抽屉 → Wikipedia 原文。关闭抽屉后仍停留在原卡片，不重置滚动、不重新抓取。原文出处要可找到，编辑标题不能冒充维基原题。

纯前端美化不需要增加推荐算法。Demo 的分类和相关条目只是体验示例，真实数据暂时不支持时隐藏，不用假标签填充。

## A / 页外 MARGIN

### 视觉概念

把百科变成一本正在翻阅的独立杂志。暖白纸底、深色中文标题、绿色图版和细分隔线建立秩序。大标题负责吸引目光，衬线英文负责调节节奏；不依赖阴影和磨砂玻璃制造层次。

桌面用左右跨页：左边文本，右边图版；两者不会彼此遮挡。底部页码与小圆点表达浏览位置。移动端重排为图在上、文字在下，而不是缩小桌面布局。

### 建议保留

- 纸色背景、窄辅助栏、图文分栏、图版编号。
- 一条知识只保留一个主按钮；收藏、分享用次级图标。
- 摘要保持独立文本区。图表与人物图片可完整展示，不强迫全屏裁剪。
- 无图条目直接成为排版卡片，不使用无关图库图片替代。

### 代价

这套方案更适合桌面和阅读，比短视频式界面少一点冲击力。中英文并置是示例视觉语言；真实数据没有英文副标题时，应删除该行并调整间距，不自动机器翻译充数。

Demo 中绿色图版属于精选词条的增强，不应要求每个随机词条都生成专属插画。正式版本要以普通照片和无图卡片为基本验收条件。

## B / 游离 DRIFT

### 视觉概念

把每条知识视为影像展中的一件展品。全幅画面、低照度界面、偏黄绿色按钮与安静的边缘操作形成“逛展”体验。文字只占画面一侧，遮罩优先保护文本区，不将整张图压成灰黑。

底部缩略图保留刚刚浏览过的条目，帮助用户回头找内容。它不是播放进度，也不会自动切页。右侧操作栏集中收藏、分享和专注看图。

### 建议保留

- 一屏一条、原生滑动、轻量顶部导航。
- 字号明确的标题与小段摘要；长内容放在抽屉。
- 少量高对比操作，其余控件退到画面边缘。
- 专注模式隐藏界面，同时让隐藏控件退出键盘焦点。

### 代价

比 A 更依赖图片质量，低清图片不适合放大铺满。实际接入时应根据图片尺寸回退为“深色画布 + 居中完整图片”，而不是放大后模糊一整屏。文字可读性优先于画面完整性；需要查看原图时提供完整图片入口。

数字标注（例如星系数量）是有来源的可选编辑增强；原版没有相应字段时，整个标注模块都不渲染。不能从摘要随意提取一个数字假装关键事实。

## 实装优先级

先完成卡片、抽屉、响应式与缺图回退，再接已有请求和收藏逻辑。随后处理长标题、语言切换、断网、图片报错和重复页。最后再加缩略轨迹、分类与少量编辑内容。

无限流只维护当前卡片附近的视图窗口，预加载下一两条图片；缓存历史位置和条目身份，不把每张高清图片永久留在 DOM。新内容追加到尾部时不改变当前索引。

不要强制把“知识钩子”作为上线条件：原题 + 摘要 + 好排版已经能够工作。固定样例上的文字长度、图片构图和事实标签不是接口契约。

## 选择建议

只选一套上线，优先 A：图文分开，更容易承受随机词条的图片和文字质量差异。移动端刷帖优先、且能做好图片回退时选 B。

也可以共用内容卡和阅读层，在桌面采用 A、移动端采用 B；但这不是简单换断点，还要统一收藏、当前条目和浏览历史。首版先做一套可减少验证成本。
